{
  "name": "mw-sports-masters-backend",
  "version": "1.0.0",
  "private": true,
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "preflight": "node -c server.js",
    "health": "node -e \"fetch('http://localhost:' + (process.env.PORT || 3001) + '/healthz').then(r => r.text()).then(t => { console.log(t); process.exit(0); }).catch(e => { console.error(e.message); process.exit(1); })\""
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.2",
    "express-rate-limit": "^7.5.0",
    "helmet": "^8.1.0",
    "mysql2": "^3.11.5",
    "xss": "^1.0.15"
  }
}
